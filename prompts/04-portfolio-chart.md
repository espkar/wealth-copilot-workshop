# Add a portfolio chart

```text
Add a responsive Recharts visualization for <allocation-or-performance>.
Reuse the existing chart components and API types. The chart must:

- handle an empty dataset
- show readable labels and tooltips
- remain usable on mobile
- avoid implying more precision than the synthetic data supports

Add or update a small frontend test where practical, then run the frontend
build and tests.
```
