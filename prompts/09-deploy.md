# Deploy the application

```text
Inspect the existing GitHub Pages workflow and Render API blueprint. Explain
how VITE_API_URL flows into the production frontend, how CORS should be
restricted for a real deployment, and which repository variables are needed.

Make only the smallest deployment changes necessary. Validate the workflow
syntax and production build. Never add credentials or hardcode secrets.
```
