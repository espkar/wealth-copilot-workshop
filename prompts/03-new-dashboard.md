# Create a dashboard capability

```text
Extend the React Wealth Copilot frontend with a new dashboard card for
<business-question>. Inspect the current components, API client and CSS
conventions first.

Add the smallest API response shape needed, keep loading and error states
consistent with the other pages, make it work after customer selection
changes, and use only fictional data. Do not introduce a state-management
library. Build and test the frontend before concluding.
```
