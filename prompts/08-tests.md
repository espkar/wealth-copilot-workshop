# Add tests

```text
Inspect the current test setup and add focused tests for <feature>. Cover
happy path, invalid customer/input, empty data and the most important derived
calculation. Reuse the existing Vitest and Supertest conventions.

Do not weaken assertions just to make tests pass. Run the smallest relevant
test command and the type-check afterward.
```
