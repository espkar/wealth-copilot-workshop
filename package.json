{
  "name": "wealth-copilot-workshop",
  "private": true,
  "version": "1.0.0",
  "description": "Workshop starter kit: Build the Wealth Copilot - a fictional Asset & Wealth Management data + AI platform demo.",
  "workspaces": [
    "api",
    "frontend"
  ],
  "scripts": {
    "generate-data": "node data/generate.mjs",
    "dev": "concurrently -n API,WEB -c blue,green \"npm run dev --workspace api\" \"npm run dev --workspace frontend\"",
    "dev:api": "npm run dev --workspace api",
    "dev:frontend": "npm run dev --workspace frontend",
    "build": "npm run build --workspace api && npm run build --workspace frontend",
    "test": "npm run test --workspace api && npm run test --workspace frontend",
    "lint": "npm run lint --workspace api && npm run lint --workspace frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
